This repository uses Git LFS for large generator input archives.
After cloning, if you need to modify the code generator, make sure to run:
```bash
git lfs install
git lfs pull
```

before running the code generator